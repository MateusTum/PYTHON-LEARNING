import math

pi = -3.14
x = 1
y = 2
z = 3

print(round(pi))
print(math.ceil(pi))
print(math.floor(pi))
print(abs(pi))     #absolute value, how far from 0
print(pow(pi,2))  #pi to the power of 2
print(math.sqrt(2))    # the square root of something
print(max(x, y, z))
print(min(x, y, z))
